spring:
  application:
    name: wms

  datasource:
    url: jdbc:postgresql://localhost:5432/wms_db
    username: wms_vaibhav
    password: root
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: Asia/Kolkata


server:
  port: 8080

